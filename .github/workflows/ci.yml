name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
        
    steps:
    - uses: actions/checkout@v4
    - name: Build and Test
      run: |
        cmake -S . -B build
        cmake --build build
        cd build && ctest --output-on-failure
